Columbia Prints Order Builder

Contents:
- index.html (current prototype)
- assets/Columbiasvgs.zip (original SVG artwork)

For GitHub Pages, upload these files to a repository.
This is a prototype. A production version should extract the SVGs into an assets folder
and load them dynamically rather than embedding everything into a single HTML file.
